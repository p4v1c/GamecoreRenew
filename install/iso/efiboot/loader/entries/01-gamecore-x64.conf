title    Install GameCore
sort-key 01
linux    /%INSTALL_DIR%/boot/x86_64/vmlinuz-linux
# One initrd, and the microcode is inside it. Both bootloaders used to be handed
# intel-ucode.img and amd-ucode.img as extra initrds from %INSTALL_DIR%/boot/,
# and mkarchiso stopped staging those files there (archiso#226) — systemd-boot
# concatenates its initrds before starting and fails the whole entry on the
# first one it cannot find ("Error preparing initrd: Not found"). The `microcode`
# hook in airootfs/etc/mkinitcpio.conf.d/archiso.conf puts both vendors' updates
# in initramfs-linux.img instead. Do not add a ucode line back here.
initrd   /%INSTALL_DIR%/boot/x86_64/initramfs-linux.img
# archisolabel rather than archisosearchuuid: both placeholders are substituted
# by mkarchiso, but the label form has worked in every archiso release, while
# the UUID form is newer. The one thing the profile must not do is depend on the
# archiso version of whoever builds it — the build happens on someone else's
# machine, and a boot failure there looks like a broken ISO, not a stale tool.
options  archisobasedir=%INSTALL_DIR% archisolabel=%ARCHISO_LABEL%
