#!/usr/bin/env bash
set -euo pipefail
# No caller-supplied paths: this is the root-owned installation manifest.
# shellcheck disable=SC1091
source /var/lib/gamecore/manifest.env
exec /bin/bash "$GAMECORE_PATH/install/steps/setup-gamecore-session.sh" \
  "$USER_NAME" "$GAMECORE_PATH" "${GAMECORE_DATA:-$GAMECORE_PATH}" "${WEB_PORT:-8765}"
