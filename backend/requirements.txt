fastapi>=0.111.0
uvicorn[standard]>=0.30.0
aiosqlite>=0.20.0
httpx>=0.27.0
python-multipart>=0.0.9
evdev>=1.6.1
requests>=2.31.0
# Overlay monitor — platform-specific (installed only on the target OS)
python-xlib>=0.33; sys_platform == "linux"
pywin32>=306; sys_platform == "win32"
argon2-cffi>=23.1.0
# Ed25519 verification for the catalogue OTA channel. Verify-only on a box:
# no private key ever reaches one. Without it the channel refuses every bundle
# rather than accepting an unverified one — see services/catalog/signing.py.
cryptography>=42.0.0
# Lossless WebP for the cover cache — services/cover_encode.py. Measured on the
# reference box: 85 PNG jackets, 42.6 MB and 605 ms of decoding, become 29.6 MB
# and 421 ms, pixel for pixel identical.
#
# Imported optionally, on purpose. install/iso/build.sh builds the offline
# wheelhouse from this file, so an ISO cut after this line carries it, but a
# box installed from an older ISO will not have it until its next OTA — and
# must keep serving its library in the meantime. Without Pillow, covers simply
# stay PNG and nothing else changes.
Pillow>=10.0.0
